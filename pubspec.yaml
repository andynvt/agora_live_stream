name: agora_live_stream
description: Agora live stream example

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  agora_rtc_engine: ^4.0.2
  permission_handler: ^8.1.2


flutter:

  uses-material-design: true
